# PowerPoint 사진 일괄 교체 프로그램

현재 **열려 있는** PowerPoint 를 그대로 제어해서, 여러 장의 사진을
**같은 위치 · 같은 크기**로 한 번에 바꿔주는 데스크톱 프로그램입니다.

- 교체할 **대상 사진들의 순서**를 정하고
- 새로 넣을 **이미지 파일들을 순서대로** 고른 뒤
- **[교체 실행]** 을 누르면 순서대로 1:1 매칭하여 교체합니다.

## 실행 환경

- **Windows** (macOS/Linux 불가 — PowerPoint COM 자동화를 사용)
- **PowerPoint 데스크톱** 설치 (Microsoft 365 / 2016 이상)
- **Python 3.8+**

## 설치

```bash
pip install -r requirements-pptx.txt
```

- `pywin32` : PowerPoint 제어 (필수)
- `Pillow` : 사진 미리보기용 (없어도 교체 기능은 동작, 미리보기만 비활성)

## 사용법

1. PowerPoint 에서 **바꾸고 싶은 사진이 있는 파일을 먼저 연다.**
2. 프로그램 실행:
   ```bash
   python pptx_image_replacer.py
   ```
3. **① PowerPoint 연결** 클릭 → 현재 열린 프레젠테이션에 연결됩니다.
4. 범위 선택(`현재 슬라이드` / `전체 슬라이드`) 후 **② 사진 불러오기** 클릭.
   - 왼쪽 목록에 대상 사진들이 나열됩니다.
   - 목록 항목을 클릭하면 **PowerPoint 화면에서 그 사진이 실제로 선택**되어
     어떤 사진인지 바로 확인할 수 있고, 가운데에 미리보기도 표시됩니다.
5. `▲ 위로 / ▼ 아래로` 로 **교체될 순서**를 정합니다.
6. 오른쪽 **파일 추가** 로 새 이미지들을 고르고, 마찬가지로 순서를 맞춥니다.
   - 왼쪽 1번 사진 ↔ 오른쪽 1번 이미지, 2번 ↔ 2번 … 식으로 매칭됩니다.
7. **③ 교체 실행** → 각 사진이 원래 자리·크기 그대로 새 이미지로 바뀝니다.

### 옵션

- **원본 비율 유지**: 체크하면 새 이미지를 원래 박스 안에 비율을 유지한 채
  가운데 맞춤으로 넣습니다. (체크 해제 시에는 요청대로 원래 박스 크기에
  정확히 채웁니다 — 비율이 다르면 늘어날 수 있음)

## 동작 방식 (요약)

- 대상 사진의 `Left/Top/Width/Height/Rotation/ZOrder` 를 기억
- 같은 위치·크기로 새 그림을 추가한 뒤 기존 사진을 삭제
- 회전값과 앞뒤 순서(z-order)까지 최대한 복원

## 참고

- 이 프로그램은 열린 문서를 **자동 저장하지 않습니다.** 결과를 확인한 뒤
  PowerPoint 에서 직접 저장(Ctrl+S)하세요. 되돌리려면 교체 직후 Ctrl+Z.
- 한 사진 교체가 실패해도 나머지는 계속 진행되며, 끝에 실패 목록을 알려줍니다.
